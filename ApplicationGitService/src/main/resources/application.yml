spring:
  autoconfigure:
    exclude: org.springframework.boot.autoconfigure.kafka.KafkaAutoConfiguration

  application:
    name: ApplicationGitService
server:
  port: 8084
eureka:
  client:
    service-url:
      defaultZone : http://localhost:9283/eureka/
