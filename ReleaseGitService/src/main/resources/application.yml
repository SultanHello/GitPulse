spring:
  jpa:
    open-in-view:
      true
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  datasource:
    url: jdbc:mysql://Localhost:3306/ReleaseGitPulseData
    username: root
    password: Zxcvbnm2006?
  application:
    name: ReleaseGitService
  data:
    redis:
      port: 6379
      host: localhost

server:
  port: 8432
eureka:
  client:
    service-url:
      defaultZone : http://localhost:9283/eureka/
